spring:
  application:
    name: ExternalSystem
  datasource:
    url: jdbc:postgresql://localhost:5432/debezium_master
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

server:
  port: 8888